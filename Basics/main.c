#include <stdio.h>

extern void pointers();
extern void memory_allocation();
extern void selection_sort_driver();
extern void permutation_driver();

int main(){
    //pointers();
    //memory_allocation();
    //selection_sort_driver();
    permutation_driver();
    return 0;
}